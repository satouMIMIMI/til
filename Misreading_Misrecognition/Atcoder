ABC258B
  問題文：上下左右および斜めの 8 方向のうちいずれかを初めに選びます。そして、好きなマスから決めた方向に 1 マス移動することを N−1 回繰り返します。
  正しい意味：最初に８方向のいずれかを決定し、好きなマスから決定した方向に1マス移動することを N−1 回繰り返す。 （例：N=４の場合、→→→と移動できる。）
  誤読：好きなマスを決定し、８方向のいずれかに １マス移動することを N-1　回繰り返す。（勘違いで N　=4の場合、→←↓）
      誤読の場合だと、マスを移動するたびに８方向の数字を比較する必要があり、B問題に出るとは思えない計算量が多いことになる。

C++言語仕様（Language specifications）
  % (mod):余剰の計算結果は負の数になることがある。必ず正の数になると思い込んでいた。

コンパイルエラーが出ない文法ミス
・foreach文
	for(int &x:AA){
      cin >> AA[x]; //AAに入力を格納できない。　
      cout << x+" ";
	}
  //正しい書き方。
      vector<int> AA(N);
	for(int &x:AA){
      cin >> x;
	}
